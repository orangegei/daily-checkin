# 每日打卡项目

一个温柔可爱的前端页面，用于记录每日增长并追踪趋势。

## 功能
- 每日打卡：可填写日期、增长值、心情备注。
- 可视化趋势：展示最近 14 天的增长曲线。
- 阶段突破分析：自动计算连续 7 天累计增长最高的时间段。
- 成长总览：累计增长、打卡天数、日均增长、最新增长。
- 本地持久化：数据保存在浏览器 `localStorage`。

## 本地运行
```bash
python3 -m http.server 4173
```
打开浏览器访问 `http://localhost:4173` 即可。
